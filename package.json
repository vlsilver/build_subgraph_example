{
  "name": "vlsilver",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ vlsilver",
    "create-local": "graph create --node http://localhost:8020/ vlsilver",
    "remove-local": "graph remove --node http://localhost:8020/ vlsilver",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vlsilver"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
